version: 2
jobs:
  build:
    docker:
      - image: golang:1.11

    steps:
      - checkout
            
      - run:
          name: build svr
          command: |
            make pre-build
            make build-image

          
workflows:
  version: 2
  build:
    jobs:
      - build